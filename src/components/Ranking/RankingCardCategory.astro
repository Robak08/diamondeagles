---
import { Badge } from "@/components/ui/badge";
const { category } = Astro.props;
---

<Badge
	variant="secondary"
	className="badge-filter bg-muted/85 select-none"
>

	{category.svg &&<Fragment set:html={category.svg} />}
	<span>
		{category.label}
	</span>
</Badge>
